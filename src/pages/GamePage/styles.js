import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    containMain: {
        flex: 1,
        backgroundColor: '#ddd',
        width: '100%',
    },
})

export default styles